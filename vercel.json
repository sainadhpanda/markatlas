{
  "version": 2,
  "buildCommand": "pnpm run build",
  "outputDirectory": "artifacts/markatlas-website/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "env": {
    "PORT": "3000",
    "BASE_PATH": "/",
    "NODE_ENV": "production"
  },
  "framework": "vite",
  "regions": ["iad1"],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
